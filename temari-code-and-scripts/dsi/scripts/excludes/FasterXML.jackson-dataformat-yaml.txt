CollectionReadTest # there are two test classes with this name with different methods, one of which always fails
ObjectIdTest # there are two test classes with this name with different methods, one of which always fails
